pub struct Slot {
    pub angle: f64,
    pub slot_length: f64,
    pub slot_width: f64,
    pub diameter: f64,
    pub depth: f64,
    pub x_coord: f64,
    pub y_coord: f64,
    pub fl_code: &str,
}
